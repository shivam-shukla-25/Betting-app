
## 1. Backend

cd backend

docker compose up -d

npm install

npm run seed

npm run dev

The API is available at `http://localhost:4000`.

The seed command prints a demo user id to use as `userId` in requests.

## 2. Frontend

cd frontend

npm install

npm run dev

The app is available at `http://localhost:3000`.
